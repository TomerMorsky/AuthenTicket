import { FC } from "react";
import styles from './logo.module.scss'

const Logo: FC = () =>{

    return (
        <div className={styles.logoContainer}>
            LOGO
        </div>
    )
}

export default Logo;